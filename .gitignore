# Node
frontend/node_modules/

# Built frontend embedded into the Go binary (regenerated by `npm run build`)
backend/web/assets/
backend/web/index.html

# Go binary
backend/kube-forwarder
kube-forwarder

# Local editor/agent settings (may contain local paths)
.claude/

# Never commit real secrets / kubeconfigs — only *.example.yaml belongs in git
deploy/secret.yaml
**/kubeconfig
**/*kubeconfig*
*.kubeconfig
kube/config
.env
.env.*
